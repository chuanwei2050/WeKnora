## 1. 领域配置与数据模型

- [ ] 1.1 定义版本化软件测评领域配置的 YAML schema、四层知识来源、默认分类层级和校验测试
- [ ] 1.2 增加知识层级、具体来源、适用范围、生效时间、失效时间和治理开关字段的数据库迁移
- [ ] 1.3 增加不可变知识版本、单一可穷举生命周期状态、审核记录和当前版本指针实体及必要唯一索引，展示标记从状态派生
- [ ] 1.4 实现知识版本与审核记录 repository，并覆盖租户和知识库作用域查询

## 2. 版本化导入与发布

- [ ] 2.1 在文档/API 导入边界解析并校验软件测评来源与时效元数据
- [ ] 2.2 实现基于内容校验和的重复检测和不可变新版本创建
- [ ] 2.3 调整解析流水线，使待审核版本可以完成解析但不会进入生产检索索引
- [ ] 2.4 实现 `approved → indexing → active/publish_failed` 即时发布事务，在全部索引就绪后原子切换当前版本并把旧版本设为 `superseded`
- [ ] 2.5 实现 `indexing → scheduled → active` 的未来生效流程、重叠有效期校验和幂等定时切换；到期时原子切换后继版本或转 `expired` 并清空当前指针
- [ ] 2.6 在检索过滤中排除未发布、未生效和已失效版本
- [ ] 2.7 实现 `superseded → indexing → active` 回滚并同步切换检索可见性的流程

## 3. 审核、权限与引用

- [ ] 3.1 增加版本创建、详情、历史、提交审核、通过、驳回、发布和回滚 API
- [ ] 3.2 接入知识库管理员权限并实现提交人不得审核自己版本的规则
- [ ] 3.3 扩展引用结果，返回知识身份、版本、知识层级、来源、页码/位置和生效时间
- [ ] 3.4 增加治理知识库的版本历史、差异摘要、审核和回滚管理界面
- [ ] 3.5 保持未启用治理的知识库沿用现有导入、索引和检索行为

## 4. 验证

- [ ] 4.1 为生命周期全部合法/非法迁移、发布失败重试、去重、权限、自审禁止、预约激活和回滚编写单元测试
- [ ] 4.2 编写集成测试证明旧版本在未来版本生效前持续可用，且只有当前已发布有效版本能被检索和引用
- [ ] 4.3 使用 PDF、Word、Excel 和 API 样例验证元数据、版本历史和引用链
- [ ] 4.4 运行现有知识导入与检索回归，确认非治理知识库无行为回退
