# Governance ES / e2e evidence — 2026-08-13

Change: `add-software-testing-knowledge-governance`

**Gate: blocked** — Elasticsearch is not available and the live e2e stack is not configured for ES-backed retrieval/indexing. No fake pass.

## 1. Elasticsearch availability

| Check | Result |
|---|---|
| `elasticsearch` service in `docker-compose.yml` | **No** — only `ELASTICSEARCH_*` env passthrough on `app` |
| Docker container on port 9200 | **None** (`docker ps` has no ES) |
| Host `http://127.0.0.1:9200/` | **Connection refused** |
| From `WeKnora-app-e2e` → `http://es:9200/` | **Unreachable** |

### Start attempt

**Not attempted.** Starting ES would require:

1. Adding an ES service (or ad-hoc container) to compose
2. Setting `RETRIEVE_DRIVER=elasticsearch_v8` and `ELASTICSEARCH_ADDR`
3. Restarting `WeKnora-app-e2e`

Current e2e app env: `RETRIEVE_DRIVER=postgres`, all `ELASTICSEARCH_*` empty. A standalone ES container on the same network would not connect to the running app without the above changes — **not low-risk**.

## 2. E2e app (`http://127.0.0.1:18080`)

| Probe | Result |
|---|---|
| Container | `WeKnora-app-e2e` — Up |
| `GET /health` | `{"status":"ok"}` |
| Login `codex-e2e-ffe6a43a902245778f7793ac8e06ddcd@example.invalid` / `OpenSpecTest1!` | **Pass** (Bearer token issued) |
| DB | `weknora_codex_e2e` on `a393146c3baa_WeKnora-postgres` |

**Can the e2e app hit ES?** No — ES is neither configured nor reachable.

## 3. Governance live data (e2e DB)

```sql
-- knowledge_bases: 4 rows, all governance.enabled = false
-- knowledge_versions: 0 rows
```

No governed knowledge base or version history exists in the e2e tenant. Full lifecycle (create → review → publish → indexed retrieval) **cannot** be exercised live without seeding data and an index backend.

## 4. Acceptance / smoke runs

### Blocked: ES-backed governance e2e

- No existing script targets live governance publish + ES retrieval.
- `internal/handler/knowledge_governance_acceptance_test.go` is in-memory SQLite only (`//go:build acceptance`).
- `vectorstore_test.go` ES connectivity cases require `http://es:9200` (same blocker noted in tasks.md §4.4).

### Partial: DocReader format acceptance (live)

Command (on compose network):

```bash
docker run --rm --network weknora-development_WeKnora-network \
  -v F:/AI-Project/WeKnora-development:/src \
  -e DOCREADER_ACCEPTANCE_ADDR=docreader:50051 \
  -w /src/docreader/client golang:1.24-bookworm \
  go test -tags acceptance -run TestGovernanceDocumentFormats -count=1 -v
```

| Format | Result |
|---|---|
| PDF | **Pass** — marker extracted, `markdown_len=23` |
| Word (docx) | **Pass** — marker extracted |
| Excel (xlsx) | **Fail** — `Failed to parse: governance.xlsx` |

## 5. PDF sample import status

| Layer | Status |
|---|---|
| DocReader PDF parse (synthetic `governance.pdf`) | **Pass** today on live docreader |
| Full governance import → version → publish → retrieval | **Not run** — no governed KB, no ES/index path on e2e stack |
| Prior evidence | `unit-governance-20260813.md` notes task 4.3 **not rerun** before today |

## 6. Verdict vs tasks.md

| Task claim | Honest status |
|---|---|
| 4.2 integration (retrieval visibility) | **Blocked** for live ES/DB e2e |
| 4.3 PDF/Word/Excel/API samples | **Partial** — PDF+Word docreader pass; Excel fail; no full import chain |
| 4.4 non-governed regression / ES vector | **Blocked** — no `es:9200` |

**Paths**

- `openspec/changes/add-software-testing-knowledge-governance/evidence/es-e2e-20260813.json`
- `openspec/changes/add-software-testing-knowledge-governance/evidence/es-e2e-20260813.md`

**Related unit evidence (pass):** `unit-governance-20260813.md`
