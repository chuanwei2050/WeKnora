## 1. 数据模型与迁移

- [x] 1.1 为 PostgreSQL 与 SQLite 的 `knowledge_tags` 增加可空 `parent_id`、索引和安全回滚迁移
- [x] 1.2 扩展文件夹领域类型与迁移测试，验证存量文件夹保持根级

## 2. 后端层级规则

- [x] 2.1 扩展创建接口和服务方法接收 `parent_id`，校验同租户、同知识库、非公共且父级必须为根级
- [x] 2.2 扩展仓储查询并在删除一级文件夹时拒绝仍有直属子级的情况
- [x] 2.3 调整根级/公共排序集合，排除普通二级文件夹且保持现有拖拽行为
- [x] 2.4 增加服务与 HTTP 测试，覆盖成功创建二级、跨库父级、公共父级、三级和父级删除保护

## 3. 前端两层展示

- [x] 3.1 扩展标签类型、创建请求与文件夹组织工具，按 `parent_id` 构造普通一级及直属二级集合
- [x] 3.2 在普通一级文件夹三点菜单中增加“新建子文件夹”，实现内联创建直属二级文件夹
- [x] 3.3 增加普通二级缩进展示，二级保留三点操作但不显示 `+` 或拖拽
- [x] 3.4 补充“新建子文件夹”及层级错误的多语言文案和前端行为测试

## 4. 验证

- [x] 4.1 执行 OpenSpec 校验、Go/前端静态检查、单元测试和生产构建
- [x] 4.2 使用真实管理员账号创建一级及二级文件夹，验证菜单、筛选、刷新持久化和三级限制并清理测试数据
- [x] 4.3 复核迁移、租户隔离、接口错误和未提交差异，确认没有引入三级或影响公共知识与未分类

## 5. 审查修复与交互补充

- [x] 5.1 为 PostgreSQL 与 SQLite 的 `parent_id` 增加限制删除的自引用外键，并补充完整性回归测试
- [x] 5.2 为普通一级文件夹增加默认展开、箭头折叠、搜索穿透和新建子级自动展开
- [x] 5.3 将普通一级、公共知识直属文件夹和同一普通父级二级文件夹的拖拽限制为各自同父级排序集合
- [x] 5.4 扩展排序 API、仓储与前端测试，覆盖二级同父级排序和跨集合拒绝
- [ ] 5.5 重新执行 OpenSpec、静态检查、单元测试、生产构建和真实账号浏览器回归
