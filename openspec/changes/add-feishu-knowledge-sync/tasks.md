## 1. 飞书能力与权限验证

- [x] 1.1 在隔离的真实飞书租户验证应用创建/读取/移动 Wiki `docx` 节点、编辑 Docx、上传/替换 File Block，并记录最小 scope、空间成员角色和限制
- [x] 1.2 验证原始附件下载一致性、页面链接稳定性、托管正文的普通搜索/知识问答表现以及墓碑化后的实际索引延迟，形成产品提示而非阻断式承诺

> 注：1.1 已全量成功同步。1.2：链接稳定性与墓碑标题已实测；飞书搜索/问答为最终一致（产品提示写入 `ops-notes.md`）；附件字节级一致性未做自动化抽检。

## 2. 数据模型与安全边界

- [x] 2.1 新增知识库级飞书配置、同步快照、运行和源到远端映射，包含 tenant/KB/target 约束、活动运行互斥和 snapshot digest 幂等键
- [x] 2.2 使用现有密钥设施加密保存当前 `app_secret`；运行保存执行所需配置副本，读取 API 只返回 `configured` 状态且不回传明文
- [x] 2.3 为配置、映射和运行 repository 增加 tenant/knowledge base scope，并覆盖跨租户和越权测试
- [x] 2.4 定义 Go 与 TypeScript 的配置、资源发现、运行和错误状态，在外部响应边界完成类型化解析

## 3. 飞书写客户端

- [x] 3.1 复用现有飞书 Connector 的 token cache、固定官方 endpoint transport 和脱敏错误处理，不改变拉取行为
- [x] 3.2 实现空间/节点分页读取、`docx` 节点创建、标题更新、节点移动和对象归属查询
- [x] 3.3 实现 Docx 托管正文块和 File Block 的创建、查询、素材上传及附件替换
- [x] 3.4 实现超时、响应大小限制、错误码映射、限流和有限指数退避，覆盖 `429`、权限失败和异步超时

## 4. 配置、资源发现与同步 API

- [x] 4.1 新增飞书配置只读 API；仅知识库所有者或租户管理员可访问，普通全局保存不得持久化飞书草稿
- [x] 4.2 新增不持久化的知识空间发现 API，使用当前表单凭据分页返回可访问空间和类型化错误
- [x] 4.3 新增 Wiki 页面树分页懒加载 API，校验节点归属并返回标题、完整路径和内部 token
- [x] 4.4 新增同步预览 API：实时校验凭据、空间和父节点，返回新增、更新、移动、墓碑化、恢复、跳过、冲突及预计上传量，不保存预览或正式配置
- [x] 4.5 新增确认同步 API：重新校验并重新计算计划；影响计数变化时返回新预览，否则在事务内保存配置、snapshot 和 queued run，并交给现有异步任务机制
- [x] 4.6 新增解除绑定 API：活动运行时拒绝，不删除飞书页面，清除当前凭据和目标并保留映射与运行历史
- [x] 4.7 检测发布目标与同知识库飞书拉取源重叠；复用现有鉴权、CSRF/Origin 和限流机制，禁止客户端 endpoint、secret 日志和跨域 token 转发

## 5. 同步规划与执行

- [x] 5.1 构建不可变快照，包含全部目录、源存在/资格状态以及文件与解析正文的权威版本、长度、MIME、名称和 digest
- [x] 5.2 实现纯规划器，区分 `deleted` 与 `temporarily_ineligible`，输出 `create`、`replace_content`、`rename`、`move`、`retire`、`restore`、`skip`、`conflict` 和预览汇总
- [x] 5.3 幂等创建托管首页、目录页和知识页，记录稳定的节点、文档、正文块、附件块、素材和源版本映射
- [x] 5.4 写入/替换原始附件和同版本可检索正文，只修改确认归属的托管块；限制正文块数和长度并标注缺失或截断
- [x] 5.5 实现名称和目录移动同步、哈希无变化跳过、父项失败阻断子项、独立分支继续及逐项成功状态提交
- [x] 5.6 实现源删除墓碑化：移除托管附件和正文、写删除提示、标记标题并移入 `_KnowledgeMesh归档`；恢复时重建托管内容和原位置
- [x] 5.7 对远端改名、移动、托管块缺失或页面删除采用 KnowledgeMesh 权威覆盖：可复用节点则改回标题/父节点并重写托管内容，不可复用则重建映射节点；不按名称接管无关页面、不覆盖用户非托管块
- [x] 5.8 实现运行状态、逐项结果、部分失败、活动运行互斥和普通再次同步的幂等补偿；任务投递失败标记为 failed
- [x] 5.9 从 snapshot 指定的租户限定不可变对象版本流式读取，校验 tenant/版本/长度/digest，禁止任意 URL，限制大小并规范化文件名
- [x] 5.10 首次成功后锁定知识空间；同空间换位置经确认后移动托管首页，跨空间必须先解除绑定

## 6. 知识库设置界面

- [x] 6.1 在已有文档知识库设置中新增独立“同步飞书”入口，隐藏该页签的全局“保存配置”按钮
- [x] 6.2 实现 `app_id`、支持显隐和“已配置”状态的 `app_secret`、“获取知识空间”、字段内错误与权限指引
- [x] 6.3 实现默认置灰的空间与同步位置选择器；凭据变化时失效，空间切换时清空节点，支持根目录、分页懒加载、面包屑和完整路径
- [x] 6.4 实现“预览同步”与影响确认；未保存关闭提醒、活动运行防重复提交及确认时影响变化后的重新确认
- [x] 6.5 展示已有目标、连接状态、当前阶段、最近运行、操作计数、失败/冲突明细和“在飞书中打开”；关闭重开后恢复运行状态
- [x] 6.6 实现解除绑定二次确认，明确既有飞书内容不会删除；补充中英文 i18n、键盘、焦点、加载和小屏布局

## 7. 验证与发布

- [x] 7.1 单元/集成测试覆盖预览确认变化、临时不合格不归档、墓碑化/恢复、哈希跳过、同名 ID、目录移动、冲突、部分失败和幂等重跑
- [x] 7.2 飞书 API mock 覆盖空间/页面树分页、凭据变化失效、校验失败不持久化、任务投递失败、限流退避、归属拒绝和秘密脱敏
- [x] 7.3 真实测试空间验收首次同步、无变化重跑、附件/正文更新、搜索表现、改名、移动、删除、恢复、用户备注保护和远端漂移冲突
- [x] 7.4 性能与结构验收（修订后范围）：真实租户基线（≥40 篇 / ~50MB）、≥4 层目录树、同名不同 ID 分建页面；不以 ≥100 篇压测或飞书搜索/问答 SLA 作为一期门禁
- [x] 7.5 增加功能开关、基础指标、结构化日志和运维说明，验证关闭开关不会删除或破坏已有飞书页面
- [x] 7.6 安全测试覆盖跨租户访问、客户端 endpoint 注入、CSRF、凭据限速、对象版本替换、日志泄密和同步回环

> 注：7.3 已覆盖首次同步、无变化重跑、replace_content、rename、删除墓碑、restore、目录 create+move、远端改名/移动覆盖、缺失节点重建。7.4 修订：41→45 篇基线、5 层 DeepL*、同名 `SameDisplayName.txt` 两页分建；≥100 篇压测与飞书检索延迟量化不做一期门禁（见 `ops-notes.md`）。
