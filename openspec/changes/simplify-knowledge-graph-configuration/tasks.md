## 1. 配置模型与边界校验

- [x] 1.1 增加三种抽取模式、模板标识、配置规范化与稳定指纹
- [x] 1.2 将图谱启用判定收敛到 `IndexingStrategy.GraphEnabled` 并兼容存量数据
- [x] 1.3 允许空 few-shot，补充完整性和模式相关 Schema 校验测试

## 2. 抽取、审核与检索链路

- [x] 2.1 让抽取任务使用规范化模式并保持通用抽取动态类型行为
- [x] 2.2 将 Agent 关系约束从 few-shot `Relations` 改为正式 `Tags`
- [x] 2.3 为待审核候选保存配置指纹并在配置、审核开关或版本变化时失效

## 3. 重建链路

- [x] 3.1 实现并注册知识库 `rebuild-index` API，按当前配置重新入队有效文档
- [x] 3.2 补充重建的权限、空知识库、重复调用和失败路径测试

## 4. 前端配置体验

- [x] 4.1 将普通区域改为启用、三模式和人工审核，并实现动态字段显示
- [x] 4.2 将专业参数、few-shot 与试运行集中到“调试与高级选项”
- [x] 4.3 新建知识库默认关闭图谱，保存回显模式并检测影响建图的配置变化
- [x] 4.4 精简说明文字并补充三模式与模板行为单元测试
- [x] 4.5 将正式 Schema 升级为带含义和关系方向的结构化定义，并降低 few-shot 的视觉与语义层级
- [x] 4.6 统一正式 Schema 与 Few-shot 的单行编辑格式，并修复空实体类型下拉项
- [x] 4.7 将正式定义与 Few-shot 统一为实体三字段、关系四字段，并移除示例单条置信度输入

## 5. 验证

- [x] 5.1 运行后端格式化、静态检查和相关单元/集成测试
- [x] 5.2 运行前端类型检查、单元测试和生产构建
- [ ] 5.3 通过真实页面验证三模式切换、保存、审核和重建提示
