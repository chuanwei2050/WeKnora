## 1. 图路径契约

- [x] 1.1 定义 `GraphQuery`、规范实体、文档实体实例、图边、关系证据、证据路径和 `GraphSearchResult` 类型
- [x] 1.2 扩展图仓储接口，增加受限路径查询并保持现有单跳接口兼容
- [x] 1.3 在认证与配置边界解析 tenant、knowledge base、允许访问的 knowledge ID，并校验关系类型、最大深度、路径数和查询预算

## 2. Neo4j 图数据模型与迁移

- [x] 2.1 以 `(tenant_id, knowledge_base_id, normalized_entity_type, normalized_name)` 定义规范实体唯一键，按受控实体类型实现带类型别名归一与歧义冲突记录，并建立文档实体实例关联、唯一约束和索引
- [x] 2.2 以知识库、源规范实体、关系类型、方向和目标规范实体定义关系身份，并将来源建模为包含 chunk、knowledge、文档版本和抽取来源的证据集合
- [x] 2.3 实现文档更新/删除时按来源增删实例和关系证据，只有无剩余证据时才清理共享关系与规范实体
- [x] 2.4 提供幂等图谱重建命令，从原始文档重新抽取并在隔离命名空间写入新模型；仅允许从权威抽取产物回填且禁止由节点属性猜测关系证据
- [x] 2.5 为切换和回滚定义计数校验、跨文档路径抽样及旧图保留策略

## 3. Neo4j 多跳查询

- [x] 3.1 实现批量一跳的逐层前沿扩展，在进入下一层前按每个父节点限制分支并累计总节点/路径预算；标签和关系类型仅由服务端枚举生成，其他值参数绑定，默认深度 2、硬上限 4
- [x] 3.2 在查询中强制 tenant、knowledge base、允许访问的 knowledge ID 和允许关系类型过滤；每条扩展边至少有一条授权关系证据且仅返回授权证据，禁止客户端提交任意 Cypher
- [x] 3.3 对种子名称执行规范化和别名解析，支持跨文档关系路径并返回真实节点、边、方向、深度和逐边证据
- [x] 3.4 实现路径预算、超时和取消，避免高分支图遍历失控
- [x] 3.5 实现确定性路径评分、循环路径、重复节点序列和重复证据去重

## 4. 检索融合与回答接入

- [x] 4.1 将图路径查询与现有关键词/向量检索并行执行并汇总错误状态
- [x] 4.2 实现图路径分数与文本检索分数的可配置融合和统一证据排序
- [x] 4.3 将逐边证据关联回知识块、文档位置和可展示引用，不生成缺少来源的路径依据
- [x] 4.4 更新 `query_knowledge_graph` 工具，返回实际节点、边、路径和引用而非空图
- [x] 4.5 将结构化证据链接入 RAG/Agent 回答上下文并限制 token 预算
- [x] 4.6 在 Neo4j 关闭、超时或无路径时回退文本检索并记录原因

## 5. 验证

- [x] 5.1 为同名不同类型实体隔离、带类型别名冲突、未知关系类型整次拒绝、关系证据增删、逐父节点分支上限、深度、关系过滤、命名空间隔离、评分和去重编写仓储单元测试
- [x] 5.2 建立跨文档软件测评图谱 fixture，验证别名对齐、两跳和三跳逐边证据链
- [x] 5.3 编写工具与聊天集成测试，校验图数据、引用和文本回退结果
- [x] 5.4 运行权限与查询预算测试，证明跨租户路径、仅由越权证据支撑的边、越权 knowledge ID 和超上限遍历被拒绝
- [x] 5.5 在已有图数据副本上执行重建演练，验证幂等、计数、抽样路径、切换与回滚

## 6. 知识治理版本联动

- [x] 6.1 为治理版本设计并实现包含 `knowledge_version_id` 的图谱 staging/active namespace，保留未治理知识库的兼容路径
- [x] 6.2 阻止 `draft`、`pending_review` 和未发布 `indexing` 版本的关系写入生产 active namespace
- [x] 6.3 将图谱 namespace 切换与向量、关键词索引及 `current_version_id` 绑定到同一发布/回滚流程，任一索引失败不得部分切换
- [x] 6.4 在 GraphContext、GraphData 和引用生成之前过滤非当前、未发布、未生效或已过期的节点、边、路径和证据
- [x] 6.5 编写待审核版本不可见、混合版本路径剔除、发布原子切换、失败保留旧版本和回滚测试
