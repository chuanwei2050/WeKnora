## 1. 服务骨架与契约

- [x] 1.1 创建 `structured-query` Python 包、`pyproject.toml`、单一 Dockerfile 和 API/Worker 启动入口
- [x] 1.2 实现严格配置模型，接收 PostgreSQL、Milvus、Redis、LLM、embedding 和安全限制配置
- [x] 1.3 定义 `/v1/datasets`、`/v1/jobs`、`/v1/query`、`/v1/health` 的 Pydantic/OpenAPI 契约与稳定错误模型
- [x] 1.4 增加生产部署示例并验证应用镜像不内置基础设施

## 2. PostgreSQL 元数据与任务系统

- [x] 2.1 建立 SQLAlchemy 2/Alembic 数据模型，覆盖 tenant、namespace、dataset、version、table、column、profile 和 job
- [x] 2.2 接入 Celery/Redis，建立幂等导入任务、状态查询、失败重试和任务锁
- [x] 2.3 实现 staging table、active version 原子切换和失败清理
- [x] 2.4 为元数据仓储、租户隔离和版本切换编写集成测试

## 3. Excel/CSV 导入

- [x] 3.1 使用 pandas/openpyxl/python-calamine 实现 CSV、XLS、XLSX 解析和工作表发现
- [x] 3.2 实现表头安全规范化、重复列处理、类型推断和原始字段映射
- [x] 3.3 将每个工作表批量写入 PostgreSQL 并生成行数、空值和类型报告
- [x] 3.4 编写多工作表、中文表头、混合类型、重复上传和失败回滚测试

## 4. Profile 与 Milvus 索引

- [x] 4.1 使用 SQLAlchemy Inspector 生成可持久化 Schema Profile，并用纯格式适配器输出 M-Schema 兼容上下文
- [x] 4.2 实现低基数字段完整 distinct 值、高基数字段有界采样及数值/日期统计
- [x] 4.3 使用官方 Milvus SDK 建立表、字段和列值向量索引，强制 tenant/namespace/version 标量过滤
- [x] 4.4 实现 Profile 重建、版本清理和容器重启恢复测试

## 5. 检索与 Text2SQL

- [x] 5.1 实现 Milvus 向量与 PostgreSQL lexical 融合检索，输出表、字段、真实值候选及分数
- [x] 5.2 实现最小充分表选择：默认一张、仅可信关系扩展、最多三张
- [x] 5.3 使用 OpenAI-compatible structured output 实现绑定数据源方言的单次主模型 SQL 生成
- [x] 5.4 编写模型 mock 契约测试，证明成功路径仅一次模型调用且无图表/总结调用

## 6. SQL 安全执行

- [x] 6.1 使用 SQLGlot 实现单条只读 SQL、授权表、最多三表及禁止危险节点校验
- [x] 6.2 实现只读事务、`EXPLAIN (FORMAT JSON)`、statement/lock timeout 和返回行数限制
- [x] 6.3 实现基于分类错误的最多一次模型修复，并保存 SQL 轨迹与模型调用计数
- [x] 6.4 编写写操作、越权表、多语句、超时、首次修复成功和二次失败测试

## 7. 可观测性与端到端验证

- [x] 7.1 实现结构化日志、Prometheus 指标和分阶段 timings，确保敏感值与密钥不进入日志
- [x] 7.2 使用真实 PostgreSQL、Milvus 和 Redis 完成上传到 SQL 执行的容器化端到端测试
- [x] 7.3 用五道真实问题记录答案、最终 SQL、证据、模型调用次数和链路耗时
- [x] 7.4 验证容器重启持久性、跨 tenant 隔离和并发查询行为
- [x] 7.5 合并 Schema Linking 与 SQL 生成，复用单次问题向量，并在词法证据充分时跳过 embedding 调用；真实浏览器复测答案与耗时

## 8. WeKnora 接入与清理

- [x] 8.1 在 WeKnora 上传链路异步提交 Excel/CSV，并持久化外部 dataset/job 标识
- [x] 8.2 在问题理解前启动结构化查询，使其与问题理解、ES/向量检索并行；由结构化服务自主返回 `sql/none`，不等待 rerank
- [x] 8.3 将 SQL 结果片段和来源接入回答汇总，并保证公司体系事实问题不误走 SQL
- [x] 8.4 将内置数据分析 Agent 和 `/api/v1/integration/tables/analyze` 从旧的“模型生成 SQL → DuckDB 执行”迁移到结构化查询服务；迁移后删除仅供旧在线问答使用的运行时代码和依赖，保留入库解析/抽样与内部元数据库安全查询
- [x] 8.5 执行代码审查、单元/集成/E2E 测试、镜像构建和 OpenSpec 校验

## 9. PostgreSQL/MySQL 零复制数据源

- [x] 9.1 定义数据源判别模型、Secret 引用、授权 database/schema/table 和字段敏感策略，并迁移元数据表
- [x] 9.2 抽象 DataSource Adapter，复用托管 PostgreSQL 执行器并实现外部 PostgreSQL Inspector、Profile 与只读执行
- [x] 9.3 实现 MySQL Adapter、方言 SQLGlot 校验、`EXPLAIN FORMAT=JSON`、只读事务和超时边界
- [x] 9.4 扩展 `/v1/datasets` 数据库注册与 Profile 刷新 Job，拒绝明文 DSN 和查询期临时连接参数
- [x] 9.5 将检索、最小表选择、Text2SQL 和执行绑定至同一 datasource/dialect，禁止跨数据源 JOIN
- [x] 9.6 增加 PostgreSQL/MySQL 零复制、白名单、敏感字段、跨租户、连接失败与 Profile 原子发布测试
- [x] 9.7 使用真实 PostgreSQL 和 MySQL 容器完成注册、Profile、查询、重启恢复及并发 E2E，并记录无业务表复制证据

## 10. 历史表格回填

- [x] 10.1 提供按租户扫描历史 CSV/XLS/XLSX 的一次性回填命令，并复用当前文件存储适配器和结构化服务配置
- [x] 10.2 支持跳过已提交文档、dry-run、并发限制、单文档重试、可重复执行和失败退出码
- [x] 10.3 增加候选筛选、上传及标识写回测试，并记录线上执行方式

## 11. Profile 元数据范围解析

- [x] 11.1 使用原始文件名和工作表名的唯一连续匹配识别显式数据范围，移除固定中文句式正则
- [x] 11.2 增加唯一文件、共享主题不误限定和工作表命中回归测试
- [x] 11.3 选表后在目标表范围内追加无模型 Value Profile 词法精检，避免全局候选被其他数据集占满
- [x] 11.4 在 SQL 执行边界按字段校验过滤字面值必须受 Profile 真实值支持，失败时仅携带有界 Profile 证据修复一次
- [x] 11.5 收紧词法查询项预算并强化首次 SQL 的 Profile 字面值自检，回归真实问题的准确率、模型调用次数和耗时
- [x] 11.6 对完整低基数 Profile 可证明恒假的 OR 分支执行 AST 等价化简，避免为不改变结果的修复增加模型调用
- [x] 11.7 主查询链路启用最小充分表选择，默认一表且仅沿已持久化可信关系扩展至最多三表
- [x] 11.8 同一数据集版本内表得分近似时提供至多一个互斥工作表候选，避免明细表与汇总表误选且不退回固定三表
- [x] 11.9 评估单次主备 SQL 与问题识别小模型，以五题准确率为准入门槛；不达标时保持单 SQL 加最多一次纠错

## 12. 通用性、准确性与规模优化

- [x] 12.1 将 SQL 字面值证据绑定升级为表限定字段，避免多表同名列串用 Profile
- [x] 12.2 对高基数采样、禁止持久化和过期外部 Profile 增加只读按需 Value Probe，且不返回或保存匹配原值
- [x] 12.3 为 Profile 名称和值增加 trigram 倒排索引，并以词法置信度而非非空结果决定是否跳过向量兜底
- [x] 12.4 复用外部 PostgreSQL/MySQL Engine 连接池，同时保持每次请求的授权表集合独立
- [x] 12.5 对超过字段预算的宽表仅裁剪首次模型上下文，修复调用自动使用完整 M-Schema
- [x] 12.6 为文件导入增加工作表、行、列资源边界，并对外部数据源字段画像使用有界并发
- [x] 12.7 验证用显式血缘替换“同结构最大表”的影响；发现真实题退化后撤回，不改变现有选表语义
- [x] 12.8 执行迁移、单元/集成测试和真实五题多轮回归，确认答案、安全边界及正常模型调用次数不退化
- [x] 12.9 提供通用执行结果评测器，按题库校验结果集、来源、模型调用和延迟，不把题目规则写入运行时

## 13. 链路收敛与入库画像质量

- [x] 13.1 将并发 lexical、置信门控和共享向量兜底封装为单一 Profile 候选检索入口，收敛查询编排复杂度
- [x] 13.2 评估合并重复 SQL Prompt 约束；真实题出现 43 人口径回归后撤回，保留完整字段和值约束
- [x] 13.3 高基数字段兼顾高频值与全域均匀候选，避免同频值只画像文件前部记录
- [x] 13.4 分批生成并写入 Profile 向量，并限制单版本 Profile 文档总量和画像任务峰值内存
- [x] 13.5 按字段数动态限制 PostgreSQL 批量写入行数，避免宽表超过绑定参数上限
- [x] 13.6 增加画像覆盖和向量分批测试，并执行完整回归与真实问题验证

## 14. 变更区审查修复

- [x] 14.1 禁止只读 SELECT 调用数据库文件、目录、延时和跨库危险函数
- [x] 14.2 为外部字段画像增加只读事务与数据库超时，并避免敏感或不可比较字段持久化原值统计
- [x] 14.3 在构造完整 DataFrame 前检查 XLSX 解压规模、工作表维度及 CSV 行列边界
- [x] 14.4 服务 API key 携带并校验实际租户，WeKnora 查询始终提交当前授权数据集白名单
- [x] 14.5 限制单次问答的结构化 namespace 并发，并将合法空结果和零聚合作为最终数据库结果返回
- [x] 14.6 外部数据库字段原值改为显式授权后才持久化，并限制单元格与结果总字节数
- [x] 14.7 执行功能、安全、全量测试及暂存区整体复审
- [x] 14.8 拒绝模型用近似名称 OR 扩大单一业务类别，避免相似证书或培训证被误计

## 15. 在线查询延迟优化

- [x] 15.1 在首次 SQL 上下文中优先限定词一致值并隔离近似冲突值，减少可避免的修复调用
- [x] 15.2 拆分元数据、lexical、embedding、向量、选表和目标表 Value 精检耗时并写入响应与轨迹
- [x] 15.3 受控评估全局三类 lexical 单次往返；因 P50/P95 退化且候选排序不等价而撤回，保留同一 Session 内的三次有界顺序查询
