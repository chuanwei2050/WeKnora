## 1. 数据模型与迁移

- [x] 1.1 为文件夹/标签增加公共归属字段，旧记录默认保持根级；不创建真实的“公共文件”标签记录
- [x] 1.2 编写并验证迁移与回滚，确保已有标签（包括同名“公共文件”标签）的名称、内容和根级归属不变

## 2. 服务端文件夹能力

- [x] 2.1 调整文件夹查询与响应结构，分别返回根级普通文件夹和公共子文件夹，不创建或返回“公共文件”真实标签
- [x] 2.2 扩展创建接口以接收受限的公共归属值，并校验编辑权限和知识库归属
- [x] 2.3 实现原子拖拽接口：同区域内只重排，跨区域时同步更新公共归属及根级、公共区域完整顺序，并校验权限和完整集合
- [x] 2.4 确认标签自动清理、文档/FAQ/导入/批量移动及数据同步等既有路径只处理真实标签，不会把虚拟容器作为标签目标
- [x] 2.5 增加服务端测试，覆盖迁移、同名普通标签、合法创建、区域内排序、拖入/拖出、事务回滚、非法集合及现有行为回归

## 3. 前端展示与交互

- [x] 3.1 扩展前端文件夹类型和状态处理，使用无真实标签 ID 的保留键构造固定“公共文件”容器及一层直属子文件夹
- [x] 3.2 在“未分类”后渲染“公共文件”，以 `+` 替代数量，并将子文件夹缩进显示在其下方
- [x] 3.3 为有编辑权限的用户接入 `+` 新建子文件夹流程，并让无编辑权限用户看不到该按钮
- [x] 3.4 实现根级与公共区域内的独立排序，以及跨边界拖入变公共、拖出取消公共的落点和视觉反馈
- [x] 3.5 在拖拽失败时恢复原位置与归属、显示错误并刷新列表，同时禁用无权限或列表加载不完整时的拖拽
- [x] 3.6 保持普通文件夹的数量、创建和筛选逻辑不变，并支持公共子文件夹筛选文档
- [x] 3.7 补充各语言所需的“公共文件”、创建及拖拽失败文案

## 4. 验证

- [x] 4.1 增加前端单元/组件测试，覆盖固定顺序、无数量有 `+`、权限、展开收起、二级缩进、点击筛选、区域内排序、跨边界归属变更、无效落点和失败回滚
- [x] 4.2 运行相关前后端测试与静态检查，并人工验证“其他文件夹行为不变”的关键路径
