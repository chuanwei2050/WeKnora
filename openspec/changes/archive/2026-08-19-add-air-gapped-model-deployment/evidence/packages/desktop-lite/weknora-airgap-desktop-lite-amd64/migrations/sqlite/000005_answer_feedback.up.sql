CREATE TABLE answer_feedback (id TEXT PRIMARY KEY, tenant_id INTEGER NOT NULL, session_id TEXT NOT NULL, message_id TEXT NOT NULL, answer_version TEXT NOT NULL, rating INTEGER NOT NULL, correction TEXT, target TEXT, status TEXT NOT NULL DEFAULT 'pending', reviewer_id TEXT, candidate_id TEXT, created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, UNIQUE (message_id, answer_version));
