## 1. 知识库编码模型

- [x] 1.1 为知识库增加可空 `code`/`code_key` 字段和 `(tenant_id, code_key)` 唯一索引，并编写存量数据无损的升级/回滚迁移
- [x] 1.2 在知识库创建与更新服务中统一实现去空白、`^[A-Za-z0-9_-]+$`、1–64 长度、大写规范化键和冲突校验
- [x] 1.3 扩展知识库 DTO、repository 查询和客户端 SDK，支持保存、读取、清除及按租户和编码大小写不敏感查找，且不对外暴露 `code_key`
- [x] 1.4 增加迁移与服务测试，覆盖空编码、合法编码、非法字符、同租户大小写冲突、跨租户同名及并发唯一约束

## 2. 知识库配置界面

- [x] 2.1 在基本信息的知识库名称下方增加“知识库编码”输入框，并显示“用于筛选知识库（可选）”提示
- [x] 2.2 接入知识库编码的加载、创建、更新、清空与前端格式校验，保持知识库描述及其他配置行为不变
- [x] 2.3 补充所有现有语言的字段名称、提示和校验错误文案，并增加组件测试

## 3. 按编码查询文件夹接口

- [x] 3.1 在 Integration API 增加 `GET /knowledge-bases/by-code/{code}/folders` 路由、稳定响应 DTO，并同时校验 `kb:list` 与 `knowledge:read` scope
- [x] 3.2 实现租户内编码解析以及 client allowlist、用户实时权限校验，对不存在和无权访问统一返回 `404`
- [x] 3.3 只查询并返回外层普通文件夹，排除“未分类”、虚拟“公共文件”及公共子文件夹，按持久化顺序稳定排序
- [x] 3.4 增加接口、鉴权和审计测试，覆盖合法结果、固定入口排除、非法编码、不存在、越权及同名跨租户隔离

## 4. RAG 文件夹范围

- [x] 4.1 扩展统一检索请求和各 retriever driver，使非空 `tag_ids` 在向量、关键词及混合检索中采用一致过滤语义
- [x] 4.2 实现可复用的文件夹范围解析器，校验显式 ID 并合并所有所选知识库的公共子文件夹，完成去重和数量上限检查
- [x] 4.3 为单次 RAG 的可选 `folder_ids` 接入范围解析；缺失、`null` 或空数组保持不筛选
- [x] 4.4 为批量 RAG 的每个 query 接入独立 `folder_ids`，并在执行任何查询前完成整批校验
- [x] 4.5 在 `knowledge_ids` 与 `folder_ids` 同时存在时校验文档属于最终文件夹范围，并让直接 Chunk 加载路径执行同一约束
- [x] 4.6 更新 Integration API 文档、客户端 SDK 和示例请求，明确未筛选、显式筛选、交集约束与公共子文件夹自动并入语义

## 5. 验证

- [x] 5.1 增加单元与集成测试，覆盖不筛选、单/多文件夹、多知识库、公共文件夹去重、未分类拒绝、跨库 ID、无效 ID、文档范围绕过和批量整批拒绝
- [x] 5.2 对所有启用的检索驱动运行标签过滤契约测试，并验证筛选前后的召回范围和全局 Top K
- [x] 5.3 运行前后端静态检查、相关全量测试、数据库迁移验证和真实 API/UI 验收
