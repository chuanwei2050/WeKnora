## 1. 语音模型与配置

- [x] 1.1 增加 TTS 模型类型、`Synthesize` 接口、配置映射和 `ModelService` 获取方法
- [x] 1.2 实现 OpenAI-compatible TTS 适配器、流式响应、超时取消和连接检查
- [x] 1.3 扩展 ASR 契约以声明流式能力，并保留现有批量 `Transcribe` 接口
- [x] 1.4 扩展智能体配置，加入语音输入/输出、ASR/TTS 模型、语言、音色和自动播放字段
- [x] 1.5 更新模型与智能体设置界面，校验模型角色和能力，保持自动播放默认关闭

## 2. 流式语音识别

- [x] 2.1 实现已认证 HTTP 票据接口，签发绑定用户、会话和用途的 WebSocket 票据；`voice.ws_ticket_ttl` 默认 60 秒、硬上限 5 分钟，并对相关 URL/日志脱敏
- [x] 2.2 在 WebSocket 升级时原子校验并消费单次票据，确保并发重放最多一个连接成功，并实现开始、音频分片、部分文本、最终文本和错误消息
- [x] 2.3 在会话边界校验音频格式、分片大小、总时长和空闲超时
- [x] 2.4 实现流式模型不可用时的临时批量 ASR 接口，复用转写能力但不创建附件或长期对象存储记录
- [x] 2.5 确保部分转写不持久化，用户确认后的最终转写通过普通消息接口发送
- [x] 2.6 实现成功/失败/取消后的即时清理，以及启动时和周期临时文件清理器；`voice.temp_max_age` 默认 1 小时、硬上限 24 小时

## 3. 前端录音与会话接入

- [x] 3.1 实现 `idle/requesting/recording/finalizing/error` 录音状态机和显式麦克风授权
- [x] 3.2 使用 `MediaRecorder` 发送受支持的音频分片并显示部分转写
- [x] 3.3 提供最终转写编辑、确认、取消和文本输入回退
- [x] 3.4 将语音来源元数据附加到普通用户消息并验证语音/文本共享多轮上下文
- [x] 3.5 在不支持 `MediaRecorder` 的浏览器禁用实时录音但保留文本与音频附件上传
- [x] 3.6 将连续对话实现为同一会话内重复的显式开始录音流程，回答结束后不得自动开启麦克风或后台监听

## 4. 合成、播放与打断

- [x] 4.1 增加鉴权 TTS 流式接口，校验回答访问权限、语言、音色和输出格式
- [x] 4.2 实现前端音频加载、播放、暂停、停止和重播状态
- [x] 4.3 在停止、开始新录音或发送新消息时取消当前 TTS 请求与播放
- [x] 4.4 保证 TTS 失败或打断不删除、不改写已持久化的文本回答和引用
- [x] 4.5 增加键盘可操作控件、状态提示和自动播放受限时的无障碍降级
- [x] 4.6 仅对有权访问的最终回答执行 TTS，默认不在消息、附件、缓存或对象存储中持久化合成音频，并在停止、结束和组件卸载时释放前端临时 URL/缓冲区
- [x] 4.7 实现显式 TTS 保留策略、期限、权限、用户提示和过期清理

## 5. 验证

- [x] 5.1 为 ASR/TTS 配置校验、票据默认期限与 5 分钟硬上限、过期、并发原子消费、重放/挪用、协议消息、资源限制、隐私和临时批量降级编写后端测试
- [x] 5.2 为录音状态机、显式连续多轮、最终确认、播放控制、打断及无后台监听编写前端组件测试
- [x] 5.3 使用线上 OpenAI-compatible ASR/TTS 跑通“语音提问—文本回答—语音播放—继续追问”基线（`evidence/online-app-voice-baseline-20260812.json`）
- [x] 5.4 使用现有音频附件和纯文本聊天执行回归，确认原路径不受影响
- [x] 5.5 输出 ASR/TTS 所需能力清单，供最终私有化变更复用同一接口做内网模型验收
- [x] 5.6 验证默认策略下流式分片、临时批量文件和 TTS 音频不会形成持久化记录，异常退出残留在最大期限内清理，且超 24 小时配置被拒绝

## 6. 端到端语音基线

- [x] 6.1 使用线上 OpenAI-compatible ASR/TTS 和真实聊天入口跑通“录音—最终转写—文本回答—TTS 播放—中断—同会话继续追问”
- [x] 6.2 记录 ASR/TTS 模型身份、文本首字、音频首块、播放中断、会话连续性和临时资源清理结果
- [x] 6.3 为双轮语音上下文、播放中断不改写文本、异常清理和默认关闭自动播放编写端到端测试
