## 1. 路由契约与配置

- [x] 1.1 定义 L1-L4、问题子类型、置信度、路由动作和降级原因的类型化结构
- [x] 1.2 增加路由启用开关、阈值、级别映射、按目标动作定义的降级链和可用能力配置并保持默认关闭
- [x] 1.3 扩展智能体配置 API 和前端表单，拒绝非法级别映射、阈值、未知动作及包含升级的降级链

## 2. 分类与决策

- [x] 2.1 扩展现有 query-understand 提示，在同一次模型调用中返回意图和复杂度结构
- [x] 2.2 实现严格 JSON 解析、枚举校验和不可解析结果的确定性回退
- [x] 2.3 将当前问题、相关历史、会话摘要、附件文本和已生成图像说明纳入分类输入，并限制输入预算
- [x] 2.4 实现 L1-L4 到快速检索、上下文检索、图谱检索和验证式回答的确定性映射
- [x] 2.5 实现从目标动作自身开始的能力感知降级，记录缺少图谱、验证器或模型能力时的实际动作且禁止升级
- [x] 2.6 实现低置信度策略，按配置选择不提高成本、权限或外部依赖的保守默认动作
- [x] 2.7 确保路由结果只缩小或选择已有权限内的知识库和工具范围

## 3. 流水线与可观测性

- [x] 3.1 将路由决策接入聊天流水线并保持未启用智能体走原路径
- [x] 3.2 为图谱检索和验证式回答预留类型化可选输入，不形成编译期循环依赖
- [x] 3.3 记录级别、子类型、置信度、计划动作、实际动作、降级原因和阶段耗时
- [x] 3.4 向客户端返回可解释的路由摘要但不暴露模型私有思维链

## 4. 验证

- [x] 4.1 为四级分类、严格解析、低置信度、每个目标动作的降级链和禁止升级编写表驱动单元测试
- [x] 4.2 编写多轮上下文测试，验证省略式追问使用历史后得到正确级别
- [x] 4.3 编写权限回归测试，证明升级路由不会访问未授权知识库或工具
- [x] 4.4 使用带 L1-L4 专家标签的线上模型样例集输出混淆矩阵和路由基线报告（`evidence/online-routing-baseline-v3.json`，准确率 95%，gate=passed）

## 5. 跨入口路由与多步执行

- [x] 5.1 抽出可复用的路由决策服务，使普通 RAG 和 Agent 入口共享同一份不可变 `RoutingDecision`
- [x] 5.2 将 `RoutingDecision` 的图谱开关、检索预算、Agent 迭代上限和验证预算传入 `AgentEngine`，移除 Agent 入口对路由结果的独立字符串覆盖
- [x] 5.3 让 `verified_agent` 实际进入 Agent/ReAct 执行分支；Agent、验证器或模型能力缺失时按降级链回退并记录实际动作
- [x] 5.4 为 L3/L4 多跳、比较、因果和假设问题实现有界 `SubQuestionPlan`，限制子问题数、调用数、总耗时并按顺序传递已确认结果
- [x] 5.5 为问题拆解、代词消解、顺序检索、预算耗尽和无图谱降级编写单元及集成测试
