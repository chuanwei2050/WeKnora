## 1. 领域配置与数据模型

- [x] 1.1 定义版本化软件测评领域配置的 YAML schema、四层知识来源、默认分类层级和校验测试
- [x] 1.2 增加知识层级、具体来源、适用范围、生效时间、失效时间和治理开关字段的数据库迁移
- [x] 1.3 增加不可变知识版本、单一可穷举生命周期状态、审核记录和当前版本指针实体及必要唯一索引，展示标记从状态派生
- [x] 1.4 实现知识版本与审核记录 repository，并覆盖租户和知识库作用域查询

## 2. 版本化导入与发布

- [x] 2.1 在文档/API 导入边界解析并校验软件测评来源与时效元数据
- [x] 2.2 实现基于内容校验和的重复检测和不可变新版本创建
- [x] 2.3 调整解析流水线，使待审核版本可以完成解析但不会进入生产检索索引
- [x] 2.4 实现 `approved → indexing → active/publish_failed` 即时发布事务，在全部索引就绪后原子切换当前版本并把旧版本设为 `superseded`
- [x] 2.5 实现 `indexing → scheduled → active` 的未来生效流程、重叠有效期校验和幂等定时切换；到期时原子切换后继版本或转 `expired` 并清空当前指针
- [x] 2.6 在检索过滤中排除未发布、未生效和已失效版本
- [x] 2.7 实现 `superseded → indexing → active` 回滚并同步切换检索可见性的流程

## 3. 审核、权限与引用

- [x] 3.1 增加版本创建、详情、历史、提交审核、通过、驳回、发布和回滚 API
- [x] 3.2 接入知识库管理员权限并实现提交人不得审核自己版本的规则
- [x] 3.3 扩展引用结果，返回知识身份、版本、知识层级、来源、页码/位置和生效时间
- [x] 3.4 增加治理知识库的版本历史、差异摘要、审核和回滚管理界面
- [x] 3.5 保持未启用治理的知识库沿用现有导入、索引和检索行为

## 4. 验证

- [x] 4.1 为生命周期全部合法/非法迁移、发布失败重试、去重、权限、自审禁止、预约激活和回滚编写单元测试
- [x] 4.2 编写集成测试证明旧版本在未来版本生效前持续可用，且只有当前已发布有效版本能被检索和引用
- [x] 4.3 使用 PDF、Word、Excel 和 API 样例验证元数据、版本历史和引用链
- [x] 4.4 运行现有知识导入与检索回归，确认非治理知识库无行为回退（Linux Docker 下 `internal/application/service/chat_pipeline` 全包回归、知识导入引用合并/内容重建用例通过；依赖 ES 的向量存储连通性用例因当前环境无 `es:9200` 单独阻塞）

## 9. 图谱版本可见性联动

- [x] 9.1 让治理版本的图谱候选携带 `knowledge_version_id` 并写入隔离 staging namespace
- [x] 9.2 阻止 `draft`、`pending_review` 和未发布 `indexing` 版本写入生产 active 图谱
- [x] 9.3 将图谱构建结果纳入发布成功条件，与向量/关键词索引和 `current_version_id` 一起原子切换，失败时保留旧版本
- [x] 9.4 在 GraphContext、结构化图结果和引用生成前过滤非当前、未发布、未生效或过期证据
- [x] 9.5 编写待发布不可见、混合版本路径移除、索引失败回退、原子发布和回滚测试
