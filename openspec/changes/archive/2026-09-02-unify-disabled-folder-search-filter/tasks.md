## 1. 统一文件夹过滤能力

- [x] 1.1 在知识服务中实现按租户和知识库计算祖先感知可搜索文件夹 ID，覆盖自身关闭、父级关闭、父子开启及异常父引用
- [x] 1.2 调整 `buildSearchTargets` 组合布尔策略、显式 `folder_ids` 与显式 `knowledge_ids`，保证合法但被开关排除的范围返回空结果而非权限错误
- [x] 1.3 让 `@` 文件搜索复用统一可搜索文件夹判定并保持过滤后的分页正确性

## 2. 后端请求契约

- [x] 2.1 为对话、Agent 和直接知识搜索请求模型增加 `filter_disabled_folders`，缺失按 `false` 传入统一检索目标构建
- [x] 2.2 为 `GET /knowledge/search` 增加同名查询参数，仅在值为 `true` 时过滤 `@` 文件列表
- [x] 2.3 为 Integration 单次 RAG 增加顶层字段，并在 `folder_ids` 解析后应用交集规则
- [x] 2.4 为 Integration 批量 RAG 的每个 query 增加字段，保证并发执行时每个 query 独立使用自己的过滤策略
- [x] 2.5 更新内部 session service 接口及测试桩，使布尔策略贯穿所有直接检索调用且不引入入口级重复过滤

## 3. 调用方与文档

- [x] 3.1 更新主对话和 `@` 列表前端调用显式传 `filter_disabled_folders=true`，保持现有产品隐藏体验
- [x] 3.2 更新悬浮窗请求显式传 `filter_disabled_folders=true`
- [x] 3.3 扩展 Go client 与前端 API 类型，使单次请求和批量 query 可按需传递字段
- [x] 3.4 更新 Swagger、API 文档和《外挂知识库指南》，说明缺省不过滤、批内独立策略、父级继承及与 `folder_ids` 的交集行为

## 4. 验证

- [x] 4.1 增加服务单元测试，覆盖两级普通文件夹、公共子文件夹、显式文件跳过、空交集和共享知识库所属租户解析
- [x] 4.2 增加 handler 测试，覆盖字段缺失、`false`、`true`、单次 RAG 与同批混合策略，并确认无效范围仍在执行前失败
- [x] 4.3 增加前端测试，验证对话、悬浮窗与 `@` 请求显式携带 `true`
- [x] 4.4 执行相关 Go/前端测试、静态检查、OpenSpec 校验及真实接口回归，确认过滤不扩大知识库授权范围
