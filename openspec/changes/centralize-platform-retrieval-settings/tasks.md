## 1. 平台配置模型与校验

- [x] 1.1 扩充 `PlatformSettings.RetrievalConfig`，加入查询扩展总开关、向量/关键词召回、RRF 权重和 rerank 候选数，并集中定义兼容旧数据的默认值
- [x] 1.2 实现完整检索策略的规范化与字段级校验，覆盖各 Top K 阶段顺序、阈值和权重边界
- [x] 1.3 补充旧平台配置缺失新增字段、非法显式更新和默认值稳定性的单元测试

## 2. 权限与配置 API

- [x] 2.1 扩展平台设置读写 DTO 和持久化逻辑，确保一次保存完整、规范化后的检索策略
- [x] 2.2 在服务端限制仅 platform workspace 的 `platform_admin` 可写检索策略，并让旧 tenant retrieval-config 写接口拒绝普通管理员覆盖
- [x] 2.3 补充 platform_admin、tenant_admin 和普通成员的配置读写权限测试

## 3. 统一运行时检索策略

- [x] 3.1 建立请求级有效检索策略解析入口，并让快速问答、智能推理、普通知识搜索和智能体复用同一不可变快照
- [x] 3.2 停止运行时读取租户和智能体历史检索覆盖字段，同时保留旧数据反序列化和非检索智能体配置行为
- [x] 3.3 统一跨知识库总召回预算，验证所有 Top K 均不随知识库数量倍增
- [x] 3.4 增加跨入口等价测试，校验召回、融合、rerank 候选、最终结果数、权重和阈值一致

## 4. Integration RAG 契约

- [x] 4.1 让单次 RAG 搜索使用平台检索策略，并将请求 `top_k` 仅作为 `min(top_k, platform.rerank_top_k)` 的响应缩小值
- [x] 4.2 让批量 RAG 的每个 query 复用相同响应上限函数，且不改变内部召回、融合和 rerank 候选预算
- [x] 4.3 更新 Integration API 文档与单次/批量契约测试，覆盖省略、为 0、小于和大于平台上限的 `top_k`

## 5. 管理界面收敛

- [x] 5.1 在平台管理员设置中新增“检索策略”入口和完整字段表单，并复用后端校验错误展示
- [x] 5.2 从知识搜索页移除租户检索策略编辑入口，确保普通管理员不再提交检索配置
- [x] 5.3 从智能体编辑器移除检索策略控件及前端默认值补齐逻辑，同时保持旧智能体其他字段可编辑
- [x] 5.4 补充相关多语言文案和前端角色可见性、表单提交、旧数据加载测试
- [x] 5.5 在平台检索策略页新增独立回答兜底分组，关闭智能体重复入口，并让快速问答按请求读取平台兜底配置

## 6. 回归验证

- [x] 6.1 运行后端单元与集成测试，验证快速问答、智能推理、智能体、知识搜索及 Integration 单次/批量流程均可完成
- [x] 6.2 运行前端类型检查与测试，并通过实际 UI 验证平台管理员唯一编辑入口及普通管理员入口关闭
- [x] 6.3 对比变更前后的授权范围、召回准确性和最终结果上限，确认未改变知识库授权、过滤、RRF 与 rerank 算法
