## 1. 评测持久化与版本

- [ ] 1.1 增加评测套件、套件版本、案例、对话轮次、覆盖标签、运行、案例结果、指标快照和人工复核的数据库迁移
- [ ] 1.2 实现评测实体 repository，保证已被运行引用的套件版本不可原地修改
- [ ] 1.3 增加套件创建、导入、冻结、版本查询和案例分层 API
- [ ] 1.4 在运行开始时保存数据集、路由分类法 ID/版本、知识内容、智能体、规范化模型身份、评估器、代码、阈值和环境快照；模型身份复用协议/提供方、规范化基础端点、模型名称和版本定义
- [ ] 1.5 将大体积运行产物写入对象存储并在数据库保存定位信息和校验和
- [ ] 1.6 定义并校验四类知识层、L1-L4 及子型、单轮/多轮、可回答/不可回答/应拒答、逐轮期望和整例聚合的案例 schema；冻结 `routing_taxonomy_id`/`routing_taxonomy_version`，分类法变化必须创建新套件版本，覆盖不足的 `research_acceptance` 版本必须拒绝冻结

## 2. 领域质量评估

- [ ] 2.1 定义统一评估器接口及分数、通过状态、理由、证据和版本输出
- [ ] 2.2 实现格式、引用存在性和引用来源一致性的确定性评估器
- [ ] 2.3 实现带固定量表的语义正确性、忠实度、引用准确性和引用完整性模型评判器
- [ ] 2.4 保存评判模型、参数、提示版本和原始结构化输出，关键配置变化时创建新版本
- [ ] 2.5 实现专家校准子集、一致性统计以及不合格评判器禁止单独承担门禁的规则
- [ ] 2.6 实现边界分数、评判冲突和抽样案例的人工复核队列及保留原始机器结果的覆盖
- [ ] 2.7 使用 `pending/incomplete/passed/failed` 可穷举门禁状态，在必需人工复核未完成时保持 `incomplete`，禁止互相矛盾的完成/通过布尔组合
- [ ] 2.8 实现按案例计算的最终准确率口径、多轮整例聚合、错误/超时计失败和不低于 90% 的内置验收门禁

## 3. 首字性能测量

- [ ] 3.1 在聊天链路统一记录请求接受、首个用户可见文本和完成时间
- [ ] 3.2 排除内部状态、思考提示和空数据块，计算并持久化每个请求的 TTFT
- [ ] 3.3 实现 15 秒逐请求门禁以及最小值、中位数、P95、最大值和超限样本统计
- [ ] 3.4 为 RAG、Agent、验证式回答和错误流编写首字边界测试
- [ ] 3.5 定义 `single-node` 与 `server-load` profile 的适用门禁、环境字段和相互隔离规则；仅当应用、全部必需数据组件和模型端点均为 `same-host` 时允许 `single-node` 通过

## 4. 五十会话与十并发工具

- [ ] 4.1 实现仓库内负载验收命令，使用 50 个独立认证虚拟用户的真实鉴权和流式聊天协议创建隔离会话
- [ ] 4.2 实现 50 个虚拟用户会话在测试窗口保持有效，并由 10 个不同用户同时提问的受控调度器
- [ ] 4.3 记录每个请求的接受、完成、超时、错误、TTFT 和总耗时
- [ ] 4.4 增加显式目标、生产确认、允许清单、用户数、并发数和持续时间安全限制
- [ ] 4.5 为调度上限、取消、错误计数和未确认生产目标编写测试
- [ ] 4.6 验证单账号多会话不会被计作多用户，且 10 个并发请求全部在配置超时内完成才通过

## 5. 报告、界面与验收

- [ ] 5.1 生成关联运行 ID 的 JSON 和 Markdown 报告并写入完整性校验和
- [ ] 5.2 在报告中包含 profile、组件位置、分类法身份、规范化模型身份、环境、版本、阈值、覆盖分布、维度指标、失败案例、引用、性能分布和复核状态
- [ ] 5.3 增加运行列表、详情、失败样本、人工复核和报告下载管理界面
- [ ] 5.4 增加源代码版本、配置、评测报告、真实截图和操作手册的材料清单与缺失状态
- [ ] 5.5 构建覆盖四类知识层、L1-L4、单轮/多轮和可回答/不可回答/应拒答的软件测评集模板，冻结逐轮与整例规则，并演练正式套件覆盖不足时拒绝冻结
- [ ] 5.6 使用线上模型执行准确率、15 秒 TTFT、50 在线会话和 10 并发基线并保存报告
- [ ] 5.7 验证 `desktop-lite` 使用批准的 `private-network` 模型时可完成适用离线功能测试，但 `single-node` 门禁失败且报告列出非同机组件
