==== std-l1-unverified-draft ttft=3215 ====
根据检索到的信息，未经验证的草稿**不能**直接发送给用户。具体规则如下：1.验证式回答必须先检索，再生成草稿。2.草稿需由两个独立模型验证事实、逻辑和引用。3.只有经过验证的草稿才能发送给用户；若证据不足，需补充检索并重新验证。

==== internal-l3-supplemental-retrieval ttft=15112 ====
根据提供的检索信息，符合验证式回答规则的流程是：**“证据不足时补充检索再验证”**。具体依据如下：1. **验证式回答的标准流程**：必须先检索，再生成草稿，然后由两个独立模型验证事实、逻辑和引用。2. **补充检索的条件**：只有在**证据不足时**，才能补充检索并重新验证。3. **禁止行为**：未经验证的草稿不能发送给用户。因此，“首次检索后直接回答”如果跳过了“由两个独立模型验证”这一关键步骤，或者在证据不足时未进行补充检索和重新验证，则不符合规则。而“证据不足时补充检索再验证”明确遵循了规则中对于证据不足情况的处理要求及验证机制。

==== std-l1-unverified-draft ====
ANSWER=根据检索到的信息，未经验证的草稿**不能**直接发送给用户。具体规则如下：1.验证式回答必须先检索，再生成草稿。2.草稿需由两个独立模型验证事实、逻辑和引用。3.只有经过验证的草稿才能发送给用户；若证据不足，需补充检索并重新验证。
CLAIM[未经验证的草稿不能发送给用户]=False
TTFT=3215
==== internal-l3-supplemental-retrieval ====
ANSWER=根据提供的检索信息，符合验证式回答规则的流程是：**“证据不足时补充检索再验证”**。具体依据如下：1. **验证式回答的标准流程**：必须先检索，再生成草稿，然后由两个独立模型验证事实、逻辑和引用。2. **补充检索的条件**：只有在**证据不足时**，才能补充检索并重新验证。3. **禁止行为**：未经验证的草稿不能发送给用户。因此，“首次检索后直接回答”如果跳过了“由两个独立模型验证”这一关键步骤，或者在证据不足时未进行补充检索和重新验证，则不符合规则。而“证据不足时补充检索再验证”明确遵循了规则中对于证据不足情况的处理要求及验证机制。
CLAIM[证据不足时才能补充检索]=False
CLAIM[未经验证的草稿不能发送给用户]=True
TTFT=15112
