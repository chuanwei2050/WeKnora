## 1. 数据模型与目录领域层

- [x] 1.1 为 PostgreSQL、SQLite、MySQL 和各初始化脚本新增 `knowledge_directories` 表，包含 `tag_id`、`parent_key`、`normalized_name`、`status`、`deletion_task_id`、租户/知识库/分类/父目录约束、跨数据库可靠的同分类同级规范名称唯一约束及父级查询索引；存量目录迁入“未分类”
- [x] 1.2 为 `knowledges` 增加可空 `directory_id` 与目录删除专用 `deletion_task_id`，并验证目录删除 CAS 与存量根级回填
- [x] 1.3 新增目录删除协调及分片记录，保存根目录、子树快照、每片稳定 `TypeKnowledgeListDelete` 任务 ID、投递/完成状态和失败摘要，不新增逐文档任务表或目录专属 worker
- [x] 1.4 新增目录 Repository 与 Service 接口，实现同租户同知识库查询、创建、改名、祖先删除锁检查、子树统计和后序删除
- [x] 1.5 在应用层实现 NFC、trim、case folding 和 `normalized_name`，覆盖危险路径、长度及深度测试
- [x] 1.6 用多数据库与并发测试验证同一路径建树幂等、规范名称同级唯一，并验证跨租户/跨知识库父级和目录关联被拒绝

## 2. 目录管理与导航 API

- [x] 2.1 增加目录路由并复用现有权限：文件上传沿用投稿权限，新建/改名/移动目录仅允许文档编辑权限，目录删除沿用批量文档删除权限，不重定义角色
- [x] 2.2 实现目录删除影响预检，按管理权限统计全部递归目录、文档和存储大小，签发服务端持久化、日志脱敏的高熵单次 `confirmation_token`，绑定操作者、子树快照摘要与有效期
- [x] 2.3 正式删除时重新校验当前租户/知识库/批量删除权限，在事务中锁定根目录、重算快照并原子消费 Token；按现有上限每 200 个文档创建带稳定 Asynq TaskID 的待投递分片，设置删除锁并复用 `MarkKnowledgeListDeleting`
- [x] 2.4 实现跨库锁协议：PostgreSQL/MySQL 按稳定顺序锁 KB sentinel 与祖先，SQLite 使用 `BEGIN IMMEDIATE`；锁后重算环，覆盖双向移动及移动-vs-删除
- [x] 2.5 为现有 `TypeKnowledgeListDelete` payload 增加协调/分片 ID；补投持久化待投递分片，worker 从协调记录加载并按租户、知识库、文档和任务 ID 做作用域 CAS，整片回写结果，全部成功后后序清理目录
- [x] 2.6 加固现有批量删除链的幂等性：重复执行或资源已不存在视为成功，配额不重复扣减
- [x] 2.7 实现分片失败语义：成功分片保留，失败分片由现有 worker 整片幂等重试并保持子树锁，只展示失败分片摘要
- [x] 2.8 限制批量删除为纯文档或纯目录选择；混合选择只支持同分类“移动到文档目录”
- [x] 2.9 扩展知识列表接口接收 `directory_id`：省略时保留旧 envelope/字段/分页/默认排序，显式目录模式按当前 `tag_id` 返回统一 `entries` 并拒绝分类与目录不一致
- [x] 2.10 将治理可见性下推到目录计数、搜索、总数和分页之前，并明确删除预检在管理员权限下统计全部文档
- [x] 2.11 扩展并复用现有 `knowledgeDisplayNameOrder`，仅修正其数据库兼容问题，追加稳定 ID 并验证各支持数据库内部翻页稳定
- [x] 2.12 实现当前分类、当前目录子树关键词搜索，结果返回只读 `directory_breadcrumb`，清除搜索恢复逐级列表
- [x] 2.13 为 CRUD、旧列表兼容、跨页、现有排序 helper、治理、撤权后确认、Token 重放/篡改、跨租户 payload、分片补投、现有删除任务重试/幂等和配额补充测试
- [x] 2.14 增加同库同分类文档/目录移动 API，校验分类归属、环、重名、权限及删除状态；批量移动全成或全不成且不更新索引；调整文档知识分类时原子清空 `directory_id`
- [x] 2.15 规范化多选移动源：折叠已选目录后代、同父级识别为无操作，并为批量操作栏和拖放复用契约

## 3. 文件夹上传与文档生命周期

- [x] 3.1 扩展现有 `KnowledgeHandler.CreateKnowledgeFromFile` / `knowledgeService.CreateKnowledgeFromFile` multipart：单文件接收当前 `tag_id + directory_id`，文件夹接收当前 `tag_id + parent_directory_id + directory_path` 且仅允许文档编辑权限；投稿权限仅允许单文件上传，服务端拒绝其文件夹路径参数；校验目录归属当前分类后继续走现有大小限制、存储、治理、去重和解析入队链路，省略目录时兼容落当前分类根级
- [x] 3.2 修改现有前端文件上传调用，提交 `webkitRelativePath` 的目录部分并继续独立提交基本文件名、当前 `tag_id` 和治理元数据，不建立第二套上传状态机
- [x] 3.3 覆盖普通上传落根级、隐藏/不支持文件过滤、部分失败留下空目录及相同内容跨目录仍按哈希去重的回归测试；重复内容不移动已有文档，并返回已有文档及其当前面包屑
- [x] 3.4 扩展现有知识库复制/同步流程映射目录树；同步已有目标库时沿用 `file_hash` 映射，仅清理本次同步新建且最终未引用的空目录；跨库移动复用现有 `MoveKnowledge` Asynq 任务及复制/同步链并重建目标目录链
- [x] 3.5 验证单文档下载、重解析、重建和治理版本切换保持目录关联，且目录字段不影响现有文件名和解析类型判断

## 4. 目录下载

- [x] 4.1 盘点现有下载/导出限制，确定并配置目录 ZIP 最大文档数与总原始大小，补充超限错误契约
- [x] 4.2 增加以目录 ID 为目标的 ZIP 聚合下载接口，校验目录自身分类归属，在查询和统计前应用治理可见性，逐文档复用现有 `GetKnowledgeFile` 授权和原文件读取，递归流式输出并支持空目录条目
- [x] 4.3 在写响应头前预检权限、目录和文件可读性，实现 Zip Slip 防护与同路径同名文件稳定消歧
- [x] 4.4 补充嵌套层级、空目录、越权/不存在目标、部分文件不可读、超限和恶意名称的目录下载测试

## 5. 右侧目录界面

- [x] 5.1 扩展现有 `KnowledgeBase.vue`、`DocumentListView.vue` 及前端 API，以统一 `entries` 延续现有分页和滚动加载，并使用稳定 `directory_id` 管理当前目录、面包屑和搜索模式
- [x] 5.2 在网格和列表视图渲染当前知识分类所属的目录项，切换分类回到其目录根级；支持点击进入、面包屑返回，目录计数限定所属分类且不随类型/关键词变化；目录固定在文档前且同样参与分页
- [x] 5.3 复用后端 `validateKnowledgeBaseAccess`、`CanContributeKnowledge`、`CanManageKnowledgeBase` 所形成的现有前端权限结果：投稿权限仅控制文件上传，文档编辑权限控制目录新建、改名、移动和拖拽，批量文档删除权限控制目录删除；不另定义角色矩阵，并处理重名、非法名称和无权限提示
- [x] 5.4 实现目录删除影响摘要、二次确认、异步进度、部分失败摘要和重试交互，删除期间禁用子树写操作
- [x] 5.5 增加目录 ZIP 下载操作和进度/失败反馈，并确保只能下载当前分类所属目录且范围由目录归属决定
- [x] 5.6 增加排序交互与 query generation/取消机制；同页单航班，成功后推进页码，切换查询丢弃旧响应；删除当前目录或祖先后导航至最近存活父级并清理面包屑
- [x] 5.7 为右侧目录使用独立 TDesign 中性线框 `folder`/`folder-open` 图标和 `document-directory-icon` 样式，禁止复用左侧蓝色 `computer-folder-icon`、渐变和拖拽手柄
- [x] 5.8 更新中英文文案与无障碍名称，将左侧命名为“知识分类”、右侧命名为“文档目录”，并补充分页、排序、目录全选和图标样式契约测试
- [x] 5.9 实现桌面文件管理器式拖放：文档和目录可放到目录项、面包屑祖先或根级；提供放置高亮、非法目标反馈和移动后 query generation 重载，不实现行间/空白处拖拽排序
- [x] 5.10 删除本变更新增的右键菜单、`Ctrl+X`/`Ctrl+V`、CutSet、剪切样式和粘贴入口，保留浏览器默认右键行为
- [x] 5.11 复用现有 `selectedIds`、批量操作栏与治理 action 判定实现统一选择策略；纯目录、纯文档和混合选择按规格显示操作，不建立第二套权限判断
- [x] 5.12 在批量操作栏提供同分类“移动到文档目录”；复用现有目录移动 API 和目标选择交互，搜索模式及删除中目标不可用
- [x] 5.13 在纯文档批量选择中保留独立“调整知识分类”入口，跨分类时原子更新 `tag_id` 并清空 `directory_id`；选择含目录时不显示该入口
- [x] 5.14 验收顶部普通入口、直接打开/预览和勾选后批量栏覆盖全部需求，且网格/列表行为一致
- [x] 5.15 补齐列表操作列和网格卡片的目录单项移动、下载、重命名、删除入口，并使列表表头全选同时覆盖当前页可操作文档与目录；验证逐项多选及纯文档、纯目录、混合批量栏动作
- [x] 5.16 为文件的列表操作列和网格卡片菜单增加短文案“移至目录”的单项入口，复用现有目录移动弹窗/API，并完整保留原“移动”菜单及其链路
- [x] 5.17 统一文件、目录单项与批量栏文案为“移至目录”和“移至分类”，不保留裸“移动”文案且不改变既有移动链路
- [x] 5.18 将右上角新建文件夹图标改为操作区内靠前的品牌色文字按钮，置于添加文档入口左侧并校验对齐和可访问名称
- [x] 5.19 将右上角添加文档图标改为与新建文件夹同高同样式的文字按钮，保留原下拉菜单和上传链路

## 6. 文档消费链回归与验收

- [x] 6.1 复用现有 `isKnowledgeDeleting`，仅在 `SearchKnowledge`、`SearchKnowledgeInScopes`、`@` 搜索和显式文档 ID 装载入口补齐排除 `deleting`；不新增活跃文档服务，不重构详情、预览、下载和历史引用装载
- [x] 6.2 增加序列化与检索契约测试，验证目录字段不进入 Chunk、索引、检索参数、RAG 范围或重排
- [x] 6.3 回归普通 RAG 与批量 RAG 的知识库范围、显式 `knowledge_ids` 和左侧 `folder_ids`；验证子目录文档召回不变，且把右侧目录 ID 传给 `folder_ids` 返回 `invalid_folder_ids`
- [x] 6.4 回归主对话、`@` 搜索/选择、Agent 和悬浮聊天框，验证子目录文档可发现且删除中文档不进入新检索
- [x] 6.5 回归引用详情、原文片段、预览和单文件下载；验证目录新增/改名不改变 `knowledge_id + chunk_id`，引用导航携带 `directory_id`/`tag_id` 打开准确目录，删除中及删除后统一显示不可用且不泄露路径
- [x] 6.6 回归递归删除时序、现有 Asynq 重试和部分失败处理，验证目录锁与文档不可检索状态原子生效，历史消息和引用记录保留
- [x] 6.7 回归文档详情、批量标签移动、治理审核、知识库复制/同步、跨库移动、重建与重新解析，确认文档身份和左侧分类语义不变
- [x] 6.8 对 PostgreSQL、SQLite 与 MySQL 分别运行迁移、目录领域、统一排序和知识列表测试
- [x] 6.9 运行前端类型检查与相关测试，手工验收完整上传、跨页、排序、CRUD、消费链、递归删除、失败重试、ZIP 和图标流程

## 7. 知识分类所属目录空间修正

- [x] 7.1 为目录 Repository/Service/API 的创建、查询、面包屑、统计、搜索、删除和移动补充 `tag_id` 作用域，并验证父子目录、文档与目录的分类一致性
- [x] 7.2 将存量目录及其关联文档安全迁入“未分类”：迁移前确保每个知识库存在未分类标签，目录写入该标签，关联文档同步调整 `tag_id`，避免产生不一致关联
- [x] 7.3 切换左侧知识分类时清除当前右侧目录、面包屑、选择和分页请求，加载目标分类根级；为空分类保留独立空目录能力
- [x] 7.4 将“调整知识分类”和“移动到文档目录”保持为两个命令；前者原子更新 `tag_id` 并清空 `directory_id`，后者只允许同分类目标
- [x] 7.5 验证文件/文件夹上传只能写入当前分类所属目录，重复文件返回其真实分类与面包屑，不因客户端伪造目录 ID 跨分类写入
- [x] 7.6 验证目录 ZIP、递归删除、知识库复制/同步和跨库移动保留或正确重映射分类归属，不信任客户端 `tag_id`
- [x] 7.7 补充 PostgreSQL、SQLite、MySQL 的分类隔离、唯一约束、存量迁移和并发测试，以及前端切换分类不串目录的契约测试
- [x] 7.8 使用真实浏览器验收：在分类 A 新建空目录和上传嵌套目录，切换分类 B 不可见；调整文档到 B 后出现在 B 根级；再验证搜索、分页、排序、移动、下载及 RAG 范围不回归
- [x] 7.9 扩展既有知识分类删除链，在删除分类记录前清空文档目录关联并后序清理所属右侧目录；`contentOnly` 保留目录结构
