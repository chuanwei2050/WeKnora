## 1. 配置与阶段契约

- [ ] 1.1 定义 `EvidenceBundle`、`DraftAnswer`、`ValidationReport`、`ReflectionPlan` 和最终回答类型
- [ ] 1.2 扩展 `CustomAgentConfig` 与运行时配置，加入启用开关、验证角色、阈值、最大反思次数和降级策略
- [ ] 1.3 增加配置持久化迁移和 API 校验，将已有 `reflection_enabled` 映射为真实运行字段
- [ ] 1.4 更新智能体配置界面，支持验证模型、严格多模型模式、阈值和反思预算
- [ ] 1.5 定义由协议/提供方、规范化基础端点、模型名称和版本组成的模型身份，拒绝用数据库记录 ID 充当独立模型
- [ ] 1.6 定义总墙钟耗时、调用次数、输入/输出 token、并行度及耗尽策略配置

## 2. 验证式回答协调器

- [ ] 2.1 实现 `VerifiedAnswerCoordinator` 并复用现有检索、Agent、模型和消息服务
- [ ] 2.2 实现草稿声明与证据 ID 的结构化绑定，拒绝无法解析的验证输入
- [ ] 2.3 实现事实、逻辑、引用和完整性验证器及严格结构化输出解析
- [ ] 2.4 并行执行独立验证角色并记录规范化模型身份、失败和降级状态
- [ ] 2.5 在严格多模型模式按规范化身份校验至少两个不同模型，重复记录或运行时不足时按策略停止或降级
- [ ] 2.6 实现确定性分数聚合、权重/阈值快照以及通过、反思、保守回答决策

## 3. 有界反思与安全输出

- [ ] 3.1 实现补充检索、重写和停止三种反思动作，并对证据与调用去重
- [ ] 3.2 强制默认一次、硬上限两次的反思预算，每轮结束后重新验证
- [ ] 3.3 在关键验证失败、证据不足或预算耗尽时生成带不确定性说明的保守回答
- [ ] 3.4 接入可选复杂度路由和图谱证据输入，缺少相关能力时仍可独立运行
- [ ] 3.5 确保所有阶段继承原会话、知识库和工具权限范围
- [ ] 3.6 实现共享总预算的原子预留与结算、阶段超时和取消传播，预算耗尽后禁止启动新调用
- [ ] 3.7 将草稿、验证意见和重写正文限制在内部事件，仅持久化并流式输出最终验证或保守回答正文

## 4. 事件、反馈与审核

- [ ] 4.1 增加不含草稿正文的阶段状态、验证摘要、反思动作、最终置信状态和耗时流式事件
- [ ] 4.2 扩展追踪记录模型身份、维度分数和降级原因，并过滤私有思维链
- [ ] 4.3 增加回答反馈、回答版本和审核状态的数据迁移与 repository
- [ ] 4.4 增加用户评分/纠错提交 API 和管理员采纳/驳回 API，从能力注册表派生当前可用的知识草稿、评测样例候选或改进工单去向
- [ ] 4.5 在采纳提交时再次校验下游能力，实现幂等候选生成及反馈、回答版本、审核人和来源关联；无可用下游时保持待审核，禁止悬空候选和自动发布
- [ ] 4.6 增加回答反馈控件和管理员审核队列，展示候选去向和后续审核状态

## 5. 验证

- [ ] 5.1 为规范化模型身份、配置校验、结构解析、聚合阈值、降级和反思上限编写单元测试
- [ ] 5.2 编写证据不足、引用冲突、验证器故障、越权补检索、总预算耗尽和请求取消的集成测试
- [ ] 5.3 使用至少两个线上模型跑通复杂问题的检索—推理—验证—反思基线并保存报告
- [ ] 5.4 运行流式与持久化测试，证明被重写的草稿不会作为用户消息泄露
- [ ] 5.5 运行反馈采纳测试，验证下游启停竞态、能力缺失、候选生成幂等，且知识草稿与评测样例不会自动发布
- [ ] 5.6 运行现有普通智能体回归，确认默认关闭时回答流程和流式协议兼容
