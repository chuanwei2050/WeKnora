## 1. 任务模型与状态存储

- [ ] 1.1 定义 `document:preview` Asynq payload、判别式任务状态、稳定错误码和内容版本去重键，并补纯函数单元测试
- [ ] 1.2 实现 Redis 预览状态存储、24 小时 TTL、原子状态迁移与 Lite 模式内存实现，并覆盖重复提交和过期测试
- [ ] 1.3 将预览 handler 注册到 Asynq 与 Lite 同步执行器，配置独立队列、并发和重试策略

## 2. 安全检测与后台转换

- [ ] 2.1 将 Office ZIP 风险检测实现为后端边界校验，覆盖 8 MiB/20 MiB/1000/20 即时预览软阈值、1 GiB/4 GiB/50000 超大文档硬阈值及 ZIP64、损坏目录、100 倍压缩比拒绝
- [ ] 2.2 实现从知识库对应存储物化原文件、创建隔离临时目录和任务结束后的可靠清理
- [ ] 2.3 使用 `exec.CommandContext` 调用LibreOffice Writer固定导出前30页PDF；普通档实施300秒/200MiB，超大档实施900秒/256MiB并验证PDF头和页数
- [ ] 2.4 成功后把版本化 PDF 写入知识库对应对象存储，完成前复核内容版本并记录产物元数据
- [ ] 2.5 覆盖成功、超时、取消、转换器缺失、输出超限、内容版本竞态和临时文件清理测试

## 3. 异步预览 API

- [ ] 3.1 实现创建或复用任务接口，复用知识库 Viewer 权限并返回 202/现有任务状态
- [ ] 3.2 实现任务状态查询与取消接口，确保 task ID 不绕过租户和知识库授权
- [ ] 3.3 实现 PDF 产物流式读取接口，支持 Range、正确 MIME 与内容版本校验，不把整份文件读入内存
- [ ] 3.4 更新路由、Swagger 与 API 文档，并增加授权、越权、过期和 Range 集成测试

## 4. 前端非阻塞预览

- [ ] 4.1 把 Office 安全 Worker 响应改为包含风险指标和 `instant/async/rejected` 判别结果
- [ ] 4.2 新增创建、查询、取消和获取预览产物的类型安全 API 客户端
- [ ] 4.3 重构 `document-preview.vue` 为显式状态机，不超过100MiB的DOCX沿用`docx-preview`，更大DOCX提交前30页任务并采用退避轮询
- [ ] 4.4 任务进行中展示阶段、进度和首批文本；成功后展示前30页PDF及下载原文件提示；失败时展示稳定错误、重试、完整文本、分块与下载入口
- [ ] 4.5 组件卸载时停止请求和轮询但不取消共享后台任务，文档内容版本变化时丢弃旧响应
- [ ] 4.6 补齐中英韩俄文案以及即时、排队、成功、失败、取消、卸载和版本切换组件测试

## 5. 下载与错误反馈

- [x] 5.1 单文件下载关闭 Axios 30 秒默认超时并增加回归测试
- [ ] 5.2 解析 Blob 错误响应并向详情页传递服务端错误消息，覆盖源文件不存在与权限错误测试
- [ ] 5.3 为超过 256 MiB 的文件新增授权下载地址/流式下载协商，前端绕过 Blob 直接触发浏览器保存并覆盖大文件测试

## 6. 超大文件上传与解析隔离

- [ ] 6.1 新增超过 256 MiB 文件的对象存储分片上传初始化、分片签名、完成校验、取消和过期清理 API
- [ ] 6.2 前端实现超大文件分片上传、并发控制、进度、重试和断点恢复，小文件保留现有 multipart 路径
- [ ] 6.3 按 256 MiB 阈值把 `document:process` 路由到超大文档独立单并发队列，设置 30 分钟及输入、内存、临时磁盘上限
- [ ] 6.4 覆盖分片中断恢复、完成校验失败、孤儿清理、解析队列隔离、超时和普通文档不受阻测试

## 7. 部署与验收

- [ ] 7.1 为 Worker 镜像安装 LibreOffice Writer/PDF 导出依赖，配置超大解析/预览独立单并发队列与 4–6 GiB 内存，并增加转换能力健康检查
- [ ] 7.2 调整本地 Nginx 大文件上传读取超时、请求缓冲策略和流式下载配置，并记录线上只读核查结果
- [ ] 7.3 增加上传、解析、预览任务时长、状态、错误码、输出大小、队列深度和清理结果的结构化日志与指标
- [ ] 7.4 在容器环境验证504.7MiB DOCX的可恢复上传、解析隔离、前30页后台预览、无完整预览入口、刷新恢复、超时与直接下载
- [ ] 7.5 运行前后端单元/集成测试、TypeScript 检查和构建，并验证关闭功能开关可安全回滚
