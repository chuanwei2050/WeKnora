# FastAPI OpenAI-compatible Rerank 服务
# 端点: POST /v1/rerank , GET /v1/models

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0
numpy>=1.24
onnxruntime>=1.17.0
# FlagEmbedding / transformers 用于加载 bge-reranker；若仅有 ONNX 权重可只用 onnxruntime
transformers>=4.40.0
tokenizers>=0.19.0
sentencepiece>=0.2.0
safetensors>=0.4.0
torch>=2.1.0
